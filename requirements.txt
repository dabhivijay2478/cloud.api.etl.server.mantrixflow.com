# Install inside the project venv to avoid conflicts with meltano/singer-sdk (e.g. run ./setup.sh or: python3 -m venv .venv && source .venv/bin/activate)
fastapi==0.128.0
uvicorn[standard]==0.35.0
pydantic==2.11.7
polars==1.33.1
python-dotenv==1.1.1
python-multipart==0.0.20
pytest==8.4.2
httpx==0.28.1
# Dependencies required by Singer tap connectors (when run via Meltano)
pendulum>=2.1.2
backoff>=1.8.0
setuptools
