{
  "version": 2,
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python",
      "config": {"maxDuration": 60}
    }
  ],
  "routes": [{"src": "/(.*)", "dest": "api/index.py"}],
  "env": {"PYTHON_VERSION": "3.11"},
  "installCommand": "pip install --upgrade pip setuptools wheel && pip install -r requirements.txt && pip install meltano && meltano install"
}
